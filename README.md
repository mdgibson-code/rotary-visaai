This code is for a widget that leverages claude ai to instruct inbound rotary exchange students about how to correctly apply for a visa
It requires a claude api key to be added in order for it to function
It would then need to be hosted on a web page, with a proxy to protect public exposure of the api key 
